name: Tasks

on: push

jobs:
    System-info: 
        runs-on: ubuntu-latest
        steps:
            - name:  Print System Info
              run: | 
               uname -a
               df -h
               date
               sudo apt-get update && sudo apt-get install -y tree
               tree -L 2 ~



                   
              
           
              
    